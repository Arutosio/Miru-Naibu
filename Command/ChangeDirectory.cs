namespace Miru_Naibu.Command
{
    public class ChangeDirectory : Command
    {
        public ChangeDirectory(string name, string type, string cmd, string description) : base(name, type, cmd, description)
        {
        }
    }
}